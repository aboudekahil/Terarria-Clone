/* This is the Sprite class which all Sprites will iherit from. This class enforces that all Sprites should
have a draw an an update method. */

class Sprite {

    constructor(isUI) {
        this.isUI = isUI;
    }
     
    draw(ctx) {

    }

    update() {
        
    }
}